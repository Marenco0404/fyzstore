{
  "name": "functions",
  "description": "Firebase Cloud Functions for F&Z Store payments (Stripe)",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "echo \"no lint\"",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0",
    "stripe": "^14.0.0",
    "cors": "^2.8.5"
  }
}